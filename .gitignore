# Ignore node_modules
node_modules/

# Ignore environment and config files
.env

# Data: track directory, ignore runtime/private files only
data/pinterest_tokens.json
data/boards.json
data/pin_counts.json
data/model_settings.json
data/*.private.txt
data/*.local.txt
# Ignore all top-level prompt chunks (prompts are not included in the repo)
data/*.txt

# Ignore build directory
build/

# Ignore WordPress sync tracking file (contains application password)
docs/.docs-sync.json
